{
  "name": "hm-web",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently --kill-others \"npm run frontend\" \"npm run server\"",
    "frontend": "cd frontend/ && npm start",
    "server": "nodemon --exec npx ts-node server/src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ducky Momo",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.4.0"
  },
  "dependencies": {
    "nodemon": "^2.0.20"
  }
}
